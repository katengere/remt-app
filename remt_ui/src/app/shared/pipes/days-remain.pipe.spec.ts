import { DaysRemainPipe } from './days-remain.pipe';

describe('DaysRemainPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysRemainPipe();
    expect(pipe).toBeTruthy();
  });
});
