import { MonthsDiffPipe } from './months-diff.pipe';

describe('MonthsDiffPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthsDiffPipe();
    expect(pipe).toBeTruthy();
  });
});
