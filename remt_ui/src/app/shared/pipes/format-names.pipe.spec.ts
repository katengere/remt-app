import { FormatNamesPipe } from './format-names.pipe';

describe('FormatNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
