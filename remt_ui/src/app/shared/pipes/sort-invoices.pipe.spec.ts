import { SortInvoicesPipe } from './sort-invoices.pipe';

describe('SortInvoicesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortInvoicesPipe();
    expect(pipe).toBeTruthy();
  });
});
